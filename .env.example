# 数据库（SQLite 文件路径）
DATABASE_URL="file:./data/jishi.db"

# JWT 密钥（生产环境必须改成随机长字符串）
# 生成：openssl rand -base64 32
JWT_SECRET="change-me-to-a-random-32-byte-string-please"

# 站点 URL（生产环境填实际域名）
NEXT_PUBLIC_APP_URL="https://ji-shi.com"

# Cloudflare Tunnel Token（仅生产部署需要）
CLOUDFLARE_TUNNEL_TOKEN=""
